# Специфично для Visual Studio Code
.vscode/
*.code-workspace

# Временные файлы операционных систем
.DS_Store
Thumbs.db

# Логи и кэш PHP
*.log
.phpunit.result.cache

# Файлы зависимостей Composer (если используете)
# Саму папку vendor на GitHub отправлять нельзя
vendor/

# Секретные файлы с паролями и доступами к БД (если есть)
.env
.env.local
config.local.php
